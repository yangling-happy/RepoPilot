server:
  port: 9000

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: business
          uri: http://localhost:8080
          predicates:
            - Path=/api/**
        - id: terminal
          uri: http://localhost:8081
          predicates:
            - Path=/ws/**
