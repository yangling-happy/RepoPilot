server:
  port: 9000

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: terminal-api
          uri: http://localhost:8081
          predicates:
            - Path=/api/terminal/**
        - id: business
          uri: http://localhost:8080
          predicates:
            - Path=/api/**
        - id: terminal
          uri: http://localhost:8081
          predicates:
            - Path=/ws/**
