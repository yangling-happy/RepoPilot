server:
  port: 9000

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: business-service
          uri: http://localhost:8080
          predicates:
            - Path=/api/**
        - id: terminal-service
          uri: http://localhost:8081
          predicates:
            - Path=/ws/**
